"use server"

export async function signIn(data) {
    try {
        // Mutation / Database /Make fetch
    } catch (error) {
        console.log(error)
    }
}

export async function signUp(userData: SignUpParams): Promise<void> {
    try {
        // Mutation / Database /Make fetch
    } catch (error) {
        console.log(error)
    }
}